package com.sopt.core.type

enum class WeekDayType(
    val koreanDay: String
) {
    SUNDAY(koreanDay = "일"),
    MONDAY(koreanDay = "월"),
    TUESDAY(koreanDay = "화"),
    WEDNESDAY(koreanDay = "수"),
    THURSDAY(koreanDay = "목"),
    FRIDAY(koreanDay = "금"),
    SATURDAY(koreanDay = "토");
}
